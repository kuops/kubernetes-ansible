---
- name: apply clusterrole
  shell: |
    /usr/local/bin/kubectl apply -f /root/approve-csr-clusterrole.yaml
    /usr/local/bin/kubectl apply -f /root/approve-csr-clusterrolebinding.yaml
  delegate_to: "{{ groups['master'][0] }}"
  run_once: true
