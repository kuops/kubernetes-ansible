---
- name: restart haproxy
  systemd:
    name: haproxy
    state: restarted
    daemon_reload: yes
    enabled: yes

- name: restart keepalived
  systemd:
    name: keepalived
    state: restarted
    daemon_reload: yes
    enabled: yes
